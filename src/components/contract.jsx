import { layout, styles } from "../utils/style"

function Contract() {
  return (
    <section className={layout.section}>
      <div className={layout.sectionInfo}>
        <h1 className={styles.heading2}></h1>
      </div>
      <div className={layout.sectionImage}></div>
    </section>
  )
}

export default Contract